# 🏗️ BluedonuLab 상세 아키텍처 설명

## 전체 시스템 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER (프론트엔드)                         │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Next.js 14 Application (React 19 + TypeScript + Tailwind CSS)  │   │
│  │                                                                   │   │
│  │  • Matching Pages (매칭 페이지)                                  │   │
│  │  • Schedule Management (일정 관리)                               │   │
│  │  • Medication Tracking (약물 추적)                               │   │
│  │  • Dashboard (대시보드)                                          │   │
│  │  • Real-time Notifications (실시간 알림)                         │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                              ↓ (HTTPS/WebSocket)                        │
└─────────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    API GATEWAY / LOAD BALANCER                           │
│                      (Azure API Management)                              │
│                                                                           │
│  • Request Routing (요청 라우팅)                                         │
│  • Authentication (인증)                                                 │
│  • Rate Limiting (속도 제한)                                             │
│  • Caching (캐싱)                                                        │
└─────────────────────────────────────────────────────────────────────────┘
                                  ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER (백엔드)                            │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │             FastAPI Application (Python 3.12)                    │   │
│  │                                                                   │   │
│  │  ┌─────────────────┐  ┌──────────────┐  ┌─────────────────────┐ │   │
│  │  │   API Routes    │  │   Services   │  │   Middleware        │ │   │
│  │  │                 │  │              │  │                     │ │   │
│  │  │ • /matching     │  │ • Matching   │  │ • CORS              │ │   │
│  │  │ • /schedule     │  │   Service    │  │ • Auth (JWT)        │ │   │
│  │  │ • /medication   │  │ • Schedule   │  │ • Error Handling    │ │   │
│  │  │ • /dashboard    │  │   Service    │  │ • Logging           │ │   │
│  │  │ • /health       │  │ • Medication │  │                     │ │   │
│  │  │                 │  │   Service    │  │                     │ │   │
│  │  └─────────────────┘  │ • Report     │  └─────────────────────┘ │   │
│  │                       │   Service    │                          │   │
│  │                       └──────────────┘                          │   │
│  │                                                                   │   │
│  │  ┌─────────────────────────────────────────────────────────┐    │   │
│  │  │         ML Integration Layer                            │    │   │
│  │  │                                                          │    │   │
│  │  │  • Model Loading (ONNX 모델 로딩)                       │    │   │
│  │  │  • Inference Engine (추론 엔진)                         │    │   │
│  │  │  • Feature Engineering (특성 엔지니어링)                │    │   │
│  │  │  • Score Calculation (점수 계산)                        │    │   │
│  │  └─────────────────────────────────────────────────────────┘    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                           │
└─────────────────────────────────────────────────────────────────────────┘
                    ↓                               ↓
     ┌──────────────────────────────┐   ┌──────────────────────────┐
     │    DATA LAYER (데이터베이스)     │   │  ML MODELS LAYER         │
     │                              │   │                          │
     │  ┌────────────────────────┐  │   │  ┌────────────────────┐  │
     │  │   PostgreSQL Database  │  │   │  │   ONNX Models      │  │
     │  │                        │  │   │  │                    │  │
     │  │ • Users (사용자)        │  │   │  │ • XGBoost          │  │
     │  │ • Patients (환자)       │  │   │  │ • Random Forest    │  │
     │  │ • Caregivers (간병인)   │  │   │  │ • Neural Network   │  │
     │  │ • Schedules (일정)      │  │   │  │ • SVM              │  │
     │  │ • Medications (약물)    │  │   │  │ • Linear Regress   │  │
     │  │ • Match Results (매칭)  │  │   │  │                    │  │
     │  │ • Dashboard Data       │  │   │  │ Location:          │  │
     │  │                        │  │   │  │ • Backend (로컬)    │  │
     │  └────────────────────────┘  │   │  │ • Azure Blob       │  │
     │                              │   │  │   Storage (배포)    │  │
     │  Data Connection: SQLAlchemy │   │  └────────────────────┘  │
     │  ORM (Python ↔ DB)           │   │                          │
     │                              │   │  Model Inference:        │
     │  Backup & Replication:       │   │  • HTTP REST API         │
     │  • Azure Backup              │   │  • gRPC (선택)           │
     │  • Geo-redundancy            │   │  • Message Queue         │
     └──────────────────────────────┘   └──────────────────────────┘

                        ↓
     ┌──────────────────────────────────────────────────────┐
     │         INFRASTRUCTURE LAYER (클라우드)               │
     │                  (Azure Cloud)                       │
     │                                                      │
     │  ┌────────────────────────────────────────────────┐ │
     │  │  Compute                                       │ │
     │  │  • App Service (Backend)                       │ │
     │  │  • Static Web Apps (Frontend)                  │ │
     │  │  • Container Instances (if needed)             │ │
     │  └────────────────────────────────────────────────┘ │
     │                                                      │
     │  ┌────────────────────────────────────────────────┐ │
     │  │  Storage                                       │ │
     │  │  • SQL Database (PostgreSQL managed)           │ │
     │  │  • Blob Storage (Models, Images, Backups)      │ │
     │  │  • Cache (Redis for Session, Real-time data)   │ │
     │  └────────────────────────────────────────────────┘ │
     │                                                      │
     │  ┌────────────────────────────────────────────────┐ │
     │  │  Monitoring & Logging                          │ │
     │  │  • Application Insights                        │ │
     │  │  • Log Analytics                               │ │
     │  │  • Azure Monitor                               │ │
     │  └────────────────────────────────────────────────┘ │
     │                                                      │
     │  ┌────────────────────────────────────────────────┐ │
     │  │  Security                                      │ │
     │  │  • Virtual Network (VNet)                      │ │
     │  │  • Network Security Groups (NSG)               │ │
     │  │  • Azure Key Vault (Secrets Management)        │ │
     │  │  • SSL/TLS Certificates                        │ │
     │  └────────────────────────────────────────────────┘ │
     └──────────────────────────────────────────────────────┘
```

---

## 1️⃣ **프론트엔드 (Frontend)**

### 기술 스택
```
Next.js 14
├── React 19
├── TypeScript 5
├── Tailwind CSS 4
├── React Hooks (useState, useEffect, useContext)
├── API Integration (fetch/axios)
└── State Management (Context API / Zustand)
```

### 주요 구성요소

#### Pages (페이지)
- **Onboarding** - 서비스 소개 및 회원가입
- **Personality Test** - 성향 진단 (4가지 축)
- **Matching Results** - AI 매칭 결과 및 간병인 선택
- **Schedule Management** - 케어 플랜 및 일정 관리
- **Medication Tracking** - 약물 관리 및 복약 지도
- **Dashboard** - 통합 대시보드 (건강 상태, 알림, 실시간 업데이트)
- **Settings** - 사용자 설정 및 프로필

#### Features (기능)
- **실시간 데이터**: WebSocket을 통한 실시간 알림 및 업데이트
- **오프라인 지원**: Service Worker를 통한 캐싱
- **반응형 디자인**: 모바일, 태블릿, 데스크톱 최적화
- **접근성**: WCAG 2.1 준수

### API 호출 흐름
```
User Action (클릭, 입력)
    ↓
React Event Handler
    ↓
API Request (fetch/axios)
    ↓
Backend Endpoint (/api/...)
    ↓
Response 처리 및 State Update
    ↓
UI Re-render
```

---

## 2️⃣ **백엔드 (Backend)**

### 기술 스택
```
FastAPI 0.104
├── Uvicorn (ASGI 서버)
├── Pydantic 2.5 (데이터 검증)
├── SQLAlchemy 2.0 (ORM)
├── Alembic (마이그레이션)
├── Python-dotenv (환경 설정)
└── Python 3.12
```

### 디렉토리 구조
```
backend/
├── main.py                    # 애플리케이션 진입점
├── requirements.txt           # 의존성
├── .env.example              # 환경변수 예제
├── app/
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py         # 환경설정, DB 연결 정보
│   │   └── security.py       # JWT 토큰, 인증
│   │
│   ├── models/               # SQLAlchemy DB 모델
│   │   ├── __init__.py
│   │   ├── user.py          # 사용자 모델
│   │   ├── patient.py       # 환자 모델
│   │   ├── caregiver.py     # 간병인 모델
│   │   ├── schedule.py      # 일정 모델
│   │   ├── medication.py    # 약물 모델
│   │   └── match_result.py  # 매칭 결과 모델
│   │
│   ├── schemas/              # Pydantic 요청/응답 스키마
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── patient.py
│   │   ├── matching.py
│   │   └── medication.py
│   │
│   ├── routes/               # API 라우트
│   │   ├── __init__.py
│   │   ├── auth.py          # 인증 엔드포인트
│   │   ├── matching.py      # 매칭 엔드포인트
│   │   ├── schedule.py      # 일정 관리 엔드포인트
│   │   ├── medication.py    # 약물 관리 엔드포인트
│   │   └── dashboard.py     # 대시보드 엔드포인트
│   │
│   ├── services/             # 비즈니스 로직
│   │   ├── __init__.py
│   │   ├── matching_service.py    # 매칭 로직
│   │   ├── schedule_service.py    # 일정 최적화
│   │   ├── medication_service.py  # 약물 검증
│   │   └── ml_service.py          # ML 모델 추론
│   │
│   ├── database/             # DB 관련
│   │   ├── __init__.py
│   │   └── connection.py     # 데이터베이스 연결
│   │
│   └── utils/                # 유틸리티
│       ├── __init__.py
│       ├── logger.py         # 로깅
│       └── validators.py     # 검증 함수
```

### API 엔드포인트 예시

#### 인증 (Authentication)
```
POST   /api/auth/register          # 회원가입
POST   /api/auth/login             # 로그인
POST   /api/auth/refresh           # 토큰 갱신
POST   /api/auth/logout            # 로그아웃
```

#### 매칭 (Matching)
```
POST   /api/matching/personality-test    # 성향 진단
POST   /api/matching/find-caregivers     # 간병인 검색
GET    /api/matching/results/{id}       # 매칭 결과 조회
POST   /api/matching/select              # 간병인 선택
```

#### 일정 (Schedule)
```
POST   /api/schedule/create              # 일정 생성
GET    /api/schedule/list                # 일정 목록
PUT    /api/schedule/{id}                # 일정 수정
DELETE /api/schedule/{id}                # 일정 삭제
GET    /api/schedule/optimize            # 일정 최적화
```

#### 약물 (Medication)
```
POST   /api/medication/add                # 약물 추가
GET    /api/medication/list               # 약물 목록
POST   /api/medication/check-interaction  # 약물 상호작용 검사
POST   /api/medication/log-taken          # 복약 기록
```

#### 대시보드 (Dashboard)
```
GET    /api/dashboard/overview            # 대시보드 개요
GET    /api/dashboard/health-status       # 건강 상태
GET    /api/dashboard/alerts              # 알림 목록
GET    /api/dashboard/medications-today   # 오늘의 약물
```

### 데이터 흐름
```
Request (프론트엔드)
    ↓
API Gateway / Load Balancer
    ↓
FastAPI Route Handler
    ↓
Middleware (인증, 로깅, CORS)
    ↓
Service Layer (비즈니스 로직)
    ├─→ ML Service (모델 추론)
    └─→ Database Query (SQLAlchemy)
    ↓
Response (JSON)
    ↓
프론트엔드 수신
```

---

## 3️⃣ **머신러닝 (Machine Learning)**

### 모델 아키텍처

#### 데이터 파이프라인
```
Raw Data (환자, 간병인 데이터)
    ↓
Data Preprocessing (전처리)
├── Missing Value Handling
├── Outlier Detection
├── Feature Scaling (정규화)
└── Categorical Encoding
    ↓
Feature Engineering (특성 엔지니어링)
├── 성향 점수 4가지 축 추출
│   ├── 공감도 (Empathy)
│   ├── 인내심 (Patience)
│   ├── 활동성 (Activity)
│   └── 자립도 (Independence)
│
├── 간병 호환도 특성
│   ├── 간병 경험
│   ├── 선호 간병 유형
│   └── 스케줄 유연성
│
└── 상호작용 특성 (Interaction Features)
    ├── 차이값 계산
    └── 거리 기반 메트릭
    ↓
Training Data (훈련 데이터)
    ↓
Model Training (모델 훈련)
├── 모델 1: XGBoost (추천 ⭐)
├── 모델 2: Random Forest
├── 모델 3: Neural Network
├── 모델 4: SVM
└── 모델 5: Linear Regression
    ↓
Model Evaluation (모델 평가)
├── R² Score
├── RMSE / MAE
├── Accuracy (±10점 범위)
└── 교차 검증
    ↓
Model Selection (최적 모델 선택)
└── XGBoost (V3: R²=0.7927, Accuracy=88.65%)
    ↓
Model Conversion (모델 변환)
└── ONNX Format (크로스 플랫폼)
    ↓
Model Deployment (모델 배포)
├── Backend (로컬 추론)
└── Azure Blob Storage (저장소)
```

#### 매칭 알고리즘 상세

**Step 1: 성향 점수 추출**
```python
patient_scores = {
    'empathy': 6.2,        # 공감도
    'patience': 7.5,       # 인내심
    'activity': 5.0,       # 활동성
    'independence': 4.8    # 자립도
}

caregiver_scores = {
    'empathy': 7.1,
    'patience': 8.2,
    'activity': 6.3,
    'independence': 5.5
}
```

**Step 2: 차이값 계산**
```
personality_differences = [
    abs(patient['empathy'] - caregiver['empathy']),          # 0.9
    abs(patient['patience'] - caregiver['patience']),        # 0.7
    abs(patient['activity'] - caregiver['activity']),       # 1.3
    abs(patient['independence'] - caregiver['independence']) # 0.7
]

avg_difference = mean(personality_differences) = 0.9
```

**Step 3: 호환도 점수 계산**
```
personality_compatibility = 100 - (avg_difference × 10) = 91점

care_compatibility = extract_care_features() = 85점
```

**Step 4: 최종 매칭 점수**
```
final_score = (personality_compatibility × 0.6) + (care_compatibility × 0.4)
            = (91 × 0.6) + (85 × 0.4)
            = 54.6 + 34
            = 88.6점 ✅ "높은 호환성"
```

### 모델 성능 비교

#### V2 모델 (시뮬레이션 데이터)
```
데이터셋: 2,000개 샘플 (생성 데이터)
└── 완벽한 패턴, 낮은 현실성

XGBoost 성능:
├── R² Score: 0.9508 ⭐⭐⭐
├── RMSE: 3.28
├── MAE: 2.15
└── Accuracy (±10점): 94.50%
```

#### V3 모델 (실제 데이터) - 현재 선택 🎯
```
데이터셋: 10,000개 샘플 (실제 인구통계 데이터)
└── 현실적 다양성, 높은 신뢰성

XGBoost 성능:
├── R² Score: 0.7927
├── RMSE: 6.42
├── MAE: 5.05
└── Accuracy (±10점): 88.65%

선택 이유:
- 실제 운영 환경에서의 신뢰성
- 다양한 시나리오 대응 가능
- Overfitting 방지
```

### 모델 배포 구조
```
ONNX Models (5개)
├── xgboost_v3.onnx          (선택 모델)
├── random_forest_v3.onnx
├── neural_network_v3.onnx
├── svm_v3.onnx
└── linear_regression_v3.onnx

저장 위치:
├── Backend (개발/테스트)
│   └── /backend/models/xgboost_v3.onnx
│
└── Azure Blob Storage (프로덕션)
    └── /models/v3/xgboost_v3.onnx

추론 방식:
1. Backend에서 모델 로드 (메모리)
2. 입력 데이터 전처리
3. ONNX Runtime으로 추론
4. 결과 반환 (JSON)
```

---

## 4️⃣ **인프라 스택 (Infrastructure)**

### 클라우드 환경 (Azure)

#### Compute Services
```
Azure App Service
├── Backend 호스팅
│   ├── Runtime: Python 3.12
│   ├── Instance Size: B2 (Standard)
│   ├── Auto-scaling: Enable
│   ├── Deployment: Git Push / CI/CD
│   └── Always On: Enabled
│
└── Frontend 호스팅
    ├── Static Web Apps
    ├── Runtime: Node.js 20
    ├── Build: Next.js
    └── CDN 통합: Enabled
```

#### Storage Services
```
Azure SQL Database
├── PostgreSQL Managed
├── Pricing Tier: General Purpose
├── Compute: 2 vCore
├── Storage: 32 GB
├── Backup: 7일 보존
└── Geo-redundancy: Enabled

Azure Blob Storage
├── Storage Account
├── Hot Tier (자주 접근하는 데이터)
│   └── ML 모델, 이미지
│
├── Cool Tier (가끔 접근)
│   └── 백업, 아카이브
│
└── Lifecycle Management:
    ├── 30일 후 Cool로 이동
    └── 90일 후 Archive로 이동

Azure Cache for Redis
├── 세션 저장소
├── 실시간 데이터 캐싱
├── Pub/Sub (실시간 알림)
└── SKU: Basic (6GB)
```

#### Networking & Security
```
Virtual Network (VNet)
├── Address Space: 10.0.0.0/16
│
├── Subnets:
│   ├── Backend: 10.0.1.0/24
│   ├── Database: 10.0.2.0/24
│   └── Storage: 10.0.3.0/24
│
├── Network Security Groups (NSG)
│   ├── Backend: HTTP/HTTPS/SSH
│   ├── Database: 5432 (PostgreSQL)
│   └── Storage: 443 (HTTPS)
│
└── Service Endpoints:
    ├── SQL Database
    └── Storage Account

Azure Key Vault
├── Database Connection String
├── JWT Secret Key
├── API Keys
├── SSL/TLS 인증서
└── 접근 제어: RBAC

Application Gateway
├── Load Balancing
├── SSL/TLS 종료
├── Web Application Firewall (WAF)
├── Path-based Routing
└── Health Probes
```

#### Monitoring & Logging
```
Application Insights
├── Application Performance Monitoring (APM)
├── Request/Response 추적
├── 예외 처리 모니터링
├── 의존성 추적
├── 성능 카운터
└── 사용자 분석

Azure Monitor
├── 메트릭 수집
├── 로그 분석
├── 알림 및 자동화
└── 대시보드

Log Analytics
├── 쿼리 언어 (KQL)
├── 로그 저장 (30일)
├── 실시간 분석
└── 통합 검색
```

### 배포 파이프라인 (CI/CD)

```
Developer Push (Git)
    ↓
GitHub Repository
    ↓
GitHub Actions Workflow 트리거
    ↓
┌─────────────────────────────────┐
│  1. Build Stage                 │
├─────────────────────────────────┤
│ • 코드 체크아웃                  │
│ • 종속성 설치                    │
│ • 코드 검사 (Linting)            │
│ • 보안 스캔                      │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  2. Test Stage                  │
├─────────────────────────────────┤
│ • 단위 테스트                    │
│ • 통합 테스트                    │
│ • 코드 커버리지 검증             │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  3. Deploy Stage (Staging)      │
├─────────────────────────────────┤
│ • Azure 스테이징 환경 배포       │
│ • 통합 테스트                    │
│ • 성능 테스트                    │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  4. Deploy Stage (Production)   │
├─────────────────────────────────┤
│ • Blue-Green 배포                │
│ • Health Check                  │
│ • 무중단 배포                    │
└─────────────────────────────────┘
    ↓
Monitoring & Alerting
```

### 환경 구성

#### Development 환경
```
로컬 개발 머신
├── Python 3.12 (가상환경)
├── Node.js 18+
├── PostgreSQL (로컬)
├── VS Code + Extensions
└── Git + GitHub CLI
```

#### Staging 환경
```
Azure App Service (Staging Slot)
├── 프로덕션과 동일한 설정
├── 테스트 데이터베이스
├── 모니터링 활성화
└── SSL/TLS 활성화
```

#### Production 환경
```
Azure App Service (Production Slot)
├── Auto-scaling Enabled
├── 실제 데이터베이스
├── 완전 모니터링
├── 백업 및 재해 복구
└── 고가용성 구성
```

### 데이터 보안 흐름

```
Client (HTTPS/TLS 1.3)
    ↓
API Gateway (WAF + DDoS Protection)
    ↓
Backend (암호화된 요청 처리)
├── JWT 토큰 검증
├── RBAC 권한 확인
└── 입력 유효성 검사
    ↓
Database (암호화된 연결)
├── 연결 문자열 (Key Vault)
├── 데이터 암호화 (TDE)
└── 백업 암호화
    ↓
ML Models (Azure Blob)
└── 접근 제어 (SAS Token)
```

---

## 5️⃣ **통합 데이터 흐름 (End-to-End)**

### 매칭 요청 전체 흐름

```
1️⃣ 사용자 입력 (Frontend)
   성향 진단 완료 → 매칭 요청 버튼 클릭

2️⃣ API 호출 (Frontend → Backend)
   POST /api/matching/find-caregivers
   {
     "patient_id": "12345",
     "personality_scores": {
       "empathy": 6.2,
       "patience": 7.5,
       "activity": 5.0,
       "independence": 4.8
     },
     "care_preferences": {...}
   }

3️⃣ Backend 처리
   a) 요청 검증
      - JWT 토큰 확인
      - 환자 정보 조회
      - 입력 데이터 검증

   b) 데이터 전처리
      - 특성 엔지니어링
      - 정규화
      - 모델 입력 형식 변환

   c) ML 모델 추론
      - ONNX 모델 로드
      - XGBoost 추론 실행
      - 매칭 점수 계산

   d) 결과 처리
      - 매칭 결과 데이터베이스 저장
      - 상위 5명 간병인 선택
      - 점수순 정렬

4️⃣ Response 반환
   {
     "matches": [
       {
         "caregiver_id": "67890",
         "name": "김미숙",
         "score": 88.6,
         "personality_match": 91,
         "care_match": 85,
         "profile": {...}
       },
       ...
     ],
     "timestamp": "2025-11-18T07:38:00Z"
   }

5️⃣ Frontend 표시
   - 매칭 결과 화면 렌더링
   - 간병인 프로필 표시
   - 호환성 점수 시각화

6️⃣ 사용자 선택
   - 간병인 선택 → 일정 관리로 진행
   - 선택 정보 데이터베이스 저장
```

### 실시간 알림 흐름

```
Backend 이벤트 발생
(약물 시간, 일정 변경 등)
    ↓
Event Processor
├── 타겟 사용자 식별
├── 메시지 생성
└── 알림 큐에 추가
    ↓
Redis Pub/Sub 또는 SignalR
├── 구독자에게 브로드캐스트
└── WebSocket 연결
    ↓
Frontend WebSocket Listener
├── 메시지 수신
└── 실시간 UI 업데이트
    ↓
사용자 알림 표시
```

---

## 6️⃣ **성능 및 확장성 고려사항**

### 캐싱 전략
```
Level 1: CDN (Frontend 정적 자산)
├── 캐시 기간: 1년
└── 대역폭 절감

Level 2: API Gateway 캐시
├── GET 요청 캐시: 5분
└── 반복 요청 성능 향상

Level 3: Redis (애플리케이션 캐시)
├── 사용자 세션: 24시간
├── 간병인 정보: 1시간
└── 매칭 결과: 30분

Level 4: Database 쿼리 최적화
├── 인덱스: user_id, patient_id, caregiver_id
├── 조인 최적화
└── 쿼리 결과 캐싱
```

### 확장성 계획
```
수평 확장 (Horizontal Scaling)
├── App Service 인스턴스 증가
├── 로드 밸런싱
└── 상태 비저장(Stateless) 설계

수직 확장 (Vertical Scaling)
├── 인스턴스 크기 증가
├── 메모리 및 CPU 업그레이드
└── 데이터베이스 컴퓨팅 티어 상향

데이터베이스 확장
├── 읽기 복제본 (Read Replicas)
├── 샤딩 (Sharding)
└── 파티셔닝 (Partitioning)
```

### SLA (Service Level Agreement)
```
Frontend 가용성: 99.9%
Backend API: 99.95%
Database: 99.99%
전체 서비스: 99.9%

응답 시간 목표:
├── API 평균: < 200ms
├── 매칭 알고리즘: < 1s
└── 대시보드 로드: < 2s
```

---

이 아키텍처 설명을 바탕으로 관계도를 그리실 수 있습니다! 🎨
